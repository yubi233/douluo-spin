# v0.3 实施过程记录

> 开始时间：2026-07-18
>
> 状态：原版转盘恢复实施中，2026-07-19
>
> 计划：`docs/v0.3重构计划.md`
>
> 工作流：`web-iterative-workflow`

## 执行约束

- 保留开始实施前已存在的未提交修改，将其业务意图合并到 v0.3，不擅自回退。
- v0.3 不兼容 v0.2 存档、转盘覆盖和内容 JSON；切换前保留旧 UI 作为基线。
- 每个缺陷先记录复现与根因，再修复并增加最低合适层级的回归测试。
- 阶段 H 严格执行单批次硬门禁，不使用循环或并行批次。

## 开始基线

| 项目 | 结果 |
|---|---|
| Git HEAD | `2d58014aa8207d7bac219ac67c340848d4e9903a` |
| 已有未提交工作 | 战后时代、自创神位、神战资格与传记补充；仍依赖旧 `machine.ts`/`engine.ts` 文本正则 |
| `npm test` | 返回 0；现有 200 轮分布测试打印 114 轮审计失败但未阻断套件 |
| `npm run build` | 通过；Vite 报告主 chunk 523.45 kB，超过 500 kB 提示线 |
| `npm run test:e2e` | 3/3 失败；Playwright 在 4173 复用了另一个本地应用，页面无 `.seed-field input` |
| 模型录屏 | 不适用；项目无模型调用，改用真实 Chrome E2E 证据 |

## 阶段 A：基线、契约与测试计划

### A-1 仓库与验证能力审计

- 已读取 `README.md`、`package.json`、`playwright.config.ts`、状态机文档、现有测试与长模拟脚本。
- 确认真实命令为 `npm test`、`npm run build`、`npm run test:e2e`、`npm run test:verify` 和 `npm run simulate:long`。
- 基线缺口 1：长模拟审计失败不使单测失败。
- 基线缺口 2：Playwright 可复用错误端口上的其他应用。

### A-2 结构化验收用例

- 新增 `docs/test/v0.3-test-cases.json`。
- 覆盖计划中 50 个稳定测试 ID，包含 P0 与关键 P1 用户旅程、内容、编辑器、存档、E2E 和最终认证。

### A-3 最小 core 与内容契约

- 新增 branded ID 构造器，只接受稳定 ASCII ID。
- 新增 GameCommand、DomainEvent、EventBatch、GameState、Predicate、NumberExpression、EffectSpec 和内容 manifest 契约。
- 新增领域错误：内容校验、非法命令、空候选、未处理 effect、流程循环和内容版本不匹配。
- 新增内容编译器，物理分离 PresentationCatalog 与 MechanicsCatalog，并输出不受文案影响的机制指纹。

### A-4 阶段 A 验证记录

| 时间 | 命令 | 结果 | 处理 |
|---|---|---|---|
| 2026-07-18 | `npx vitest run tests/core/contentCompiler.test.ts` | 6 项行为断言初次通过 | 继续运行生产类型检查 |
| 2026-07-18 | `npm run build` | 发现测试 fixture 直接改写 readonly 契约 | 保留生产 readonly，将测试变更限定到显式可变断言 |
| 2026-07-18 | `npm run test:e2e` | 独占端口后 2/3 通过；桌面流程发现“进入下一项”会立即再抽取 | 将人工推进与抽取分离，保留自动推进逻辑 |
| 2026-07-18 | `npm run test:verify` | 10 文件、78 项 Vitest，生产构建和 3 项 Chrome E2E 全部通过 | 阶段 A 退出门禁通过 |

## 阶段 B：Walking Skeleton

### B-1 实施目标

- 引入 XState 5 作为外层命令权限状态图，不在 action 中修改领域实体。
- 实现显式 RNG、候选分布、effect 编译、reducer、Process Manager 稳定循环和 EventBatch 原子提交。
- 以结构化内容跑通人类性别、容貌、武魂类型和一个具体武魂。
- 实现按 turnId 截断重放的回退，收据锁定 UI 落点和动画。

### B-2 继续实施时的基线复核

| 时间 | 命令 | 结果 | 根因与处理 |
|---|---|---|---|
| 2026-07-18 | `npm run test:core` | 17 项通过，1 项失败 | XState `toJSON()` 结果包含循环对象，架构测试的无环递归导致 `Maximum call stack size exceeded`；加入 `WeakSet` 去重后重新验证。 |

### B-3 Walking Skeleton 完成与阶段 C-E 纵向扩展

- Walking Skeleton 的人类性别、容貌、武魂类型和具体武魂均由结构化 effect 与 signal 驱动。
- 新增显式成长次数、魂环序号、剧情节点、雷劫阈值、路线选择和神考进度，未使用全局 flags。
- 新增 `HumanProgressionProcess`、`SoulRingProcess`、`StoryTimelineProcess`、`BeastCultivationProcess`、`GodTrialProcess` 与 `EndingProcess`。
- 内容包扩展为人类成长、魂兽、化形、魂环、剧情、神考、死亡和终局纵向切片。

### F-1 旧内容一次性结构化迁移

| 时间 | 命令 | 结果 |
|---|---|---|
| 2026-07-18 | `npx vite-node scripts/migrate-v03-legacy-content.ts` | 旧数据 276 个池、2,500 个选项全部生成稳定 ASCII ID；每项显式写入 `enabled`、正权重和 `effects: []`，输出 `src/content/v03/legacyContent.generated.json`。 |

迁移策略：历史池默认归类为纯叙事审阅内容，不从文案推导机制；主旅程使用单独审阅过的结构化池。生成资产接入后成为内容唯一事实来源，主 UI 切换完成时删除旧 `src/data/wheels.json` 和一次性迁移脚本。

## 阶段 B-G：事件内核、内容迁移与 UI 接入

后续实现已经完成事件驱动内核、主要 Process Manager、v0.3 内容迁移、事件日志存档、共享投影和 Vue UI 主入口切换。完整的暂停时事实、验证状态与剩余工作统一记录在 `docs/v0.3-resume-checkpoint.md`，避免在流水日志中维护第二份恢复清单。

## 2026-07-19 暂停归档

- 用户要求暂停并归档，线程 goal 已切换为 paused。
- 终止仍在运行的 `npm run test:e2e`；Playwright 结果为 interrupted，4 项未运行，不能计为通过。
- 没有启动新的实现、长模拟预演或最终认证。
- 当前代码仍在基线提交 `2d58014aa8207d7bac219ac67c340848d4e9903a` 上的未提交工作树中。
- 恢复入口：`docs/v0.3-resume-checkpoint.md`。

## 2026-07-19 恢复：阶段 G 完整化

### G-1 浏览器阻断修复

- 修复 `useGameStore` 中未依赖 revision 的 Vue computed：启动后旧的 `isStarted=false` 使开始对话框继续遮挡页面。现在所有服务投影都通过响应式 `serviceState` 更新。
- E2E 的 `start()` 增加开始对话框隐藏断言，防止遮罩回归。
- CDP screencast 改为 `try/finally` 停止，测试失败也不会留下挂起会话；继续使用本机 ffmpeg 合成真实 Chrome MP4。

### G-2 结构化编辑器和 patch 导入

- 新增 predicate、数值表达式和 effect 三类递归编辑器，覆盖 all/any/not/compare/contains/policy、constant/fact/add/multiply/min/max/clamp/policy 以及 6 种注册 effect。
- 预览通过临时内容完整重编译和真实候选分布计算，不用编辑器内的近似公式。
- 新增两池 patch 导出/导入、版本/未知池/重复池/结构校验和原子失败处理。
- `ContentService` 在 Vue Proxy 边界深拷贝 patch，消除 `structuredClone` 的 `DataCloneError`；服务错误回显到编辑器。
- 增加桌面和移动真实浏览器用例，并修复移动机制摘要与删除按钮重叠。

### G-3 契约、内容和架构加固

- 新增 `ContentService` 5 项测试：文案不改变机制指纹、纯叙事空 effects、结构预览不提交、两池往返、非法导入原子拒绝。
- 内容编译器增加稳定 ASCII ID 和 entity effect/reference 类型校验。由此发现并修复魂兽 species 被写入 `beast-bloodline` 的类型错误，增加独立血脉实体，内容版本升级到 `v0.3.0-dev.5`。
- effect 编译使用 reducer 的实际边界值，重复 grant/revoke 不再制造虚假事实事件；增加上下界、幂等、未知 effect 原子失败、流程循环原子失败测试。
- 新增生产架构扫描，覆盖 48 个 source 文件，阻断旧 domain、`wheels.json`、旧 v1 key 和文本解析器回流。
- `test:verify` 已包含内容校验、41 项 Vitest、生产构建、架构扫描和 7 项 Playwright。

### G-4 首次预演与缺陷闭环

- generation：`simulation-archives/v03-preflight-20260719/dev5-working-tree`。
- batch-01 生成 20 份传记、20 份 JSON、20 份审阅；自动审计 20/20。未关闭 batch-01 gate 时尝试 batch-02，脚本按预期拒绝，验证单批次硬门禁。
- 逐份阅读全文并核对 JSON 后，人工结果为 6 passed、14 failed、0 pending：
  - `PRE-FLIGHT-001`：10/10 人类样本的 `pool.story.1` 早于 `pool.setup.faction`。
  - `PRE-FLIGHT-002`：4/4 化形样本的首个 `pool.human.soul-ring` 早于 `pool.setup.appearance` 与 `pool.setup.faction`。
- 根因分别为剧情 Process 无冒险阶段限制、魂环 Process 在 `setup.transformed` 响应等级变化。旧 generation 状态置为 failed 并完整保留证据。
- 修复后使用原失败 seed 增加两项顺序集成测试；旧死亡夹具因合法任务顺序改变而改用已确认死亡的 `v03-human-28`，核心与 E2E 同步。

### G-5 修复后回归

| 时间 | 命令 | 结果 |
|---|---|---|
| 2026-07-19 | `npm run test:verify` | 内容 309 pools / 2,601 options / 63 entities；41/41 Vitest；build 通过；48 文件架构扫描通过；7/7 Chrome E2E 通过 |

构建仍提示主 chunk 753.93 kB（gzip 141.23 kB）超过 500 kB；这是非阻断性能项，不影响当前功能与认证正确性门禁。

### G-6 第二次预演与初始魂环顺序修复

- generation：`simulation-archives/v03-preflight-20260719/dev5-working-tree-r2`。
- 新 batch-01 的 20/20 自动审计和终局完整性通过，前两项 setup 顺序缺陷已消失。
- 逐份审阅结果为 7 passed、13 failed、0 pending。确认 `PRE-FLIGHT-003`：全部 10 个人类样本和 3 个化形样本在进入冒险后先执行初次成长，再补初始魂环。
- 代表样本 15 为先天 20 级，但先进入“两年后的成长与遭遇”并死亡，最终 20 级、0 魂环；这违反阶段 C 的“等级 → 魂环/魂骨 → 成长/遭遇”。r2 代次作废并保留全部证据。
- 修复将初次成长排程从 `CharacterSetupProcess` 移交 `HumanProgressionProcess`。流程稳定循环中 `SoulRingProcess` 先响应冒险阶段事件，故初始魂环先于成长进入 agenda。
- `simulateJourney` 自动审计新增三组 `invalid-task-order` 检查：势力/剧情、化形设定/魂环、初始魂环/成长。使用原失败 seed 增加集成回归，并用构造错误 trace 验证自动审计确实能阻断。
- 修复后 `npm run test:verify` 再次通过：42/42 Vitest、build、48 文件架构扫描、7/7 Chrome E2E。

### G-7 第三次预演与魂兽身份组合修复

- generation：`simulation-archives/v03-preflight-20260719/dev5-working-tree-r3`。
- 新 batch-01 的 20/20 自动审计、已知顺序规则与终局完整性通过。
- 逐份审阅结果为 14 passed、6 failed、0 pending。确认 `PRE-FLIGHT-004`：魂兽类型、种族与生存区域独立抽取，产生“海魂兽 + 疾风魔狼”或“陆生魂兽 + 灵潮鲸”等非法组合。
- 新增 `actor.beast-types`、`actor.beast-species`、`actor.beast-areas` 集合事实；内容编译器、规则求值器和结构化编辑器统一注册。种族按类型过滤，区域按种族过滤。
- 模拟自动审计新增最终类型/种族/血脉/区域一致性检查，并用构造非法 state 验证审计会失败。
- 内容版本升级到 `v0.3.0-dev.6`，机制指纹从 `34b0d76a` 变为 `405b4f33`，条件选项由 33 增至 37。
- 首次全门禁的唯一失败是 E2E 仍断言旧内容版本；同步为 dev.6 后整套 `npm run test:verify` 通过：43/43 Vitest、build、48 文件架构扫描、7/7 Chrome E2E。

### G-8 第四次预演通过

- generation：`simulation-archives/v03-preflight-20260719/dev6-working-tree-r4`。
- batch-01：20/20 自动审计、20/20 人工逐份 passed、`no-defect-confirmed`、`npm run test:verify` 通过，gate closed。
- batch-02 只在 batch-01 关闭后单独生成；20/20 自动审计、20/20 人工逐份 passed、`no-defect-confirmed`、`npm run test:verify` 通过。
- 40 份 JSON 均完成终局、agenda 为空、最终事件为 `run.finished`；覆盖人类死亡/继承成神/自创神位、魂兽死亡/飞升和多条化形路径。
- 预演累计人工审阅 100 份：前三代分别发现并修复 2、1、1 个阻断缺陷，第四代连续 40 份无确认缺陷。

### G-9 候选冻结准备

- README 与状态机文档从 v0.1 旧架构改写为当前事件内核、内容服务、存档、编辑器和认证事实。
- 自动化文档完成 50 个验收 ID 覆盖审计：45 个已由自动化/预演关闭，5 个等待最终 400 份认证。
- 应用版本冻结为 `0.3.0`，内容 manifest 冻结为 `v0.3.0`；正式冻结门禁通过并创建候选提交后，不再修改代码或内容。

### G-10 正式冻结门禁

| 检查 | 冻结结果 |
|---|---|
| 内容审计 | `v0.3.0`，指纹 `fc2023a1`，309 pools / 2,601 options / 63 entities / pending 0 |
| Vitest | 7 files，43/43 passed |
| 生产构建 | passed；主 chunk 755.17 kB，gzip 141.41 kB，保留 >500 kB 性能提示 |
| 架构扫描 | 48 个生产源文件 passed |
| Chrome E2E | 7/7 passed，trace/截图/MP4 证据已生成 |

候选提交将显式强制包含被仓库旧 `.gitignore` 规则忽略的 `tests/core`、`tests/application` 和 `e2e/v0.3.spec.ts`。

## 2026-07-19 阶段 H：首次正式认证代次失效

### H-1 原候选与前 12 批通过

- 候选提交：`1338d1b61f993ffb97b3bfcb41dedcf6fddf72a0`，generation：`1338d1b-v0.3.0`，seed 前缀：`v03-final-1338d1b`。
- batch-01 至 batch-12 严格逐批生成、逐份检查 20 个 JSON 与 20 份完整传记、填写独立 review，再执行 `verify-gate`；12 批均为 20/20 自动审计、20/20 人工 passed、`no-defect-confirmed`。
- 此阶段累计完成 240 份正式认证传记审阅，没有并行生成批次，也没有在前一批 gate 关闭前生成下一批。

### H-2 FINAL-001：继承成神提前跳过主线 4

- batch-13 的 20 份 JSON 自动审计均通过；人工逐份审阅到 sample-12（seed `v03-final-1338d1b-batch-13-sample-12`）时确认阻断缺陷。
- 该样本由魂兽化形进入人类成长，连续高额突破后提前达到 100 级；归档只包含主线节点 1~3，却已完成 3 次继承神考并进入 `ending.god-ascension`，传记缺少“剧情4”。
- 根因是 `HumanProgressionProcess` 将等级达到 99 视为立即进入终局的充分条件，而剧情任务依靠成长产生的时间推进排程；自动审计也缺少继承成神必须完成 4 个主线节点的规则。
- 修复保持单一流程责任：等级已满但主线不足 4 节时继续安排成长，让时间推进补齐剧情；后续成长完成且主线齐全后才允许启动神考或战后路线。`simulateJourney` 同时新增 `missing-story-node` 审计兜底。
- 使用原 seed 增加集成回归，验证化形路线完成 4 个主线节点、3 次继承神考并正常成神。

### H-3 旧代次封存与修复回归

- batch-13 最终人工结果为 19 passed、1 failed、0 pending；未执行通过 gate，未生成 batch-14。
- 原 generation 保留 13 批全部原始产物，manifest 与 batch-13 gate 标记为 `generation-invalidated`，finding 位于 `batch-13/findings/FINAL-001.md`。
- 原代次累计审阅 260 份：259 passed、1 failed；其中 sample-12 证明自动审计不能替代逐份人工语义审阅。
- 定向测试通过 44/44；随后 `npm run test:verify` 全门禁通过：内容 `v0.3.0` / 指纹 `fc2023a1` / 309 pools / 2,601 options / 63 entities / pending 0，44/44 Vitest，生产构建，48 文件架构扫描，7/7 Chrome E2E。
- 生产构建仍仅保留主 chunk 755.35 kB（gzip 141.44 kB）超过 500 kB 的非阻断提示。
- 按 V03-CERT-001，修复提交形成新的候选代次，最终认证必须从 batch-01 完整重跑，旧 12 个 passed 批次不计入最终 400 份。

### H-4 最终有效代次：20 x 20 认证完成

- 最终有效候选：commit `45609b276fb26d901f84a04df355fc034e9590be`，contentVersion `v0.3.0`，generation `45609b2-v0.3.0`，seed 前缀 `v03-final-45609b2`。
- batch-01 至 batch-20 均按“单独生成 -> 20 份传记和 JSON 全文/字段交叉审阅 -> 20 份独立 review -> verify-gate”的顺序串行完成。任一批关闭前未生成下一批。
- 最终证据审计：20/20 gate passed，400/400 自动审计通过，400/400 传记完整，400/400 JSON 归档完整，400/400 独立审阅为 passed，0 pending，0 failed；路线配比为 200 human 和 200 beast。
- batch-19/sample-11 的 112 级死亡样本经检查为有效行为：`level` 的 reducer 注册边界是 1..159，`max-level=99` 是流程进入神考的阈值，不是通用属性上限。该结论记录在对应 review，未修改候选代码或内容。
- 最后一次 batch-20 `verify-gate` 重新通过完整门禁：内容审计 309 pools / 2,601 options / 63 entities / pending 0，44/44 Vitest，生产构建，48 文件架构扫描，7/7 Chrome E2E。构建仍有 796.48 kB raw / 156.53 kB gzip 主 chunk 的非阻断提示。
- 总结报告：`simulation-archives/v03-final-20260719/45609b2-v0.3.0/{manifest.json,audit-report.md,manual-review-report.md}`；逐批证据位于同目录 `batch-01` 至 `batch-20`。

### H-5 原版剧情转盘恢复：从原始数据重新迁移

- 用户确认 `1338d1b` 删除的 `src/data/wheels.json` 包含关键剧情数据；此前“276 池/2,500 选项的文案已保留”并不成立为完整迁移，因为生成物把所有历史选项写为 `effects: []`，且没有接入原版规则语义。
- 恢复源固定为 `1338d1b^:src/data/wheels.json`，机械快照保存为 `src/content/v03/legacyContent.source.json`；SHA-256 为 `a15531384c18d428622596d1d96b645ded8a8d65e14e3d948c1c33ad1c024b1b`，包含 276 个池、2,500 个选项和 25 个标签。
- 新增 `scripts/migrate-v03-legacy-content.ts` 与 `npm run content:migrate`。该脚本只在构建期读取原版文案，生成 `legacyContent.generated.json` 的稳定 ID、标签实体、称号/领域实体、显式 `stat.change`、`entity.grant`、`entity.revoke`、`run.finish` 和 `availableWhen`；生产运行时仅加载生成内容，不读取原版 JSON，也不解析中文文本。
- 首轮生成结果：477 个选项携带效果、784 个选项携带资格条件；其中效果共 376 个属性变化、120 个实体授予、2 个实体撤销和 44 个死亡终局。原版池和选项数量逐项守恒。`tests/content/legacyMigration.test.ts` 锁定原版 SHA、总数及等级、称号、死亡、条件样本。
- 282 条依赖武魂属性、领域数量、路线专属状态、剧情计数或组合门槛的复杂旧条件不能安全降级为通用 predicate，完整索引生成在 `test-results/iterations/v0.3/legacy-migration/audit.json`；它们已显式列为恢复 backlog，不再以 `pending: 0` 掩盖。
- `PoolBrowser` 的内容域由“历史纯叙事”改为“原版迁移内容”，独立投掷结果会显示迁移出的效果和条件摘要。
- 内容语义变更将 package 与 manifest 从 `0.3.0` / `v0.3.0` 升级为 `0.3.1` / `v0.3.1`。因此 `45609b2-v0.3.0` 的 400 份认证保留为历史证据，但按 V03-CERT-001 已不能作为当前版本发布认证；恢复完成后必须从 batch-01 重跑。
# 2026-07-19：原 v0.2 领域规则恢复基线

- 确认 `1338d1b` 删除的并不只是 `wheels.json`，而是完整的 `src/domain`：11 个模块、2,969 行。
- 从其父提交 `2d58014` 归档 `biography`、`canonAdditions`、`catalog`、`engine`、`machine`、`martialSoulCategories`、`martialSoulTiers`、`overrides`、`random`、`simulation` 与 `types` 到 `legacy/v02-source/domain/`。
- 为归档写入逐文件 SHA-256 清单 `legacy/v02-source/MANIFEST.sha256`，作为后续迁移的不可变来源校验；生产源和构建产物不得导入该目录。
- 新增 `docs/v0.3-legacy-rule-migration.md`。该矩阵记录了每个删除模块的原有规则、当前 v0.3 缺口、目标架构落点与验收方式。静态规则、规则策略、流程调度、投影与模拟按依赖顺序恢复。
- 结论：先前的 `legacyContent.generated.json` 恢复了 276 个原始盘面，但没有恢复动态增补、武魂分类/品阶、专用资格/战力、完整任务排程或审计规则。当前版本仍不得称为完整迁移，也不得重启发布认证。

# 2026-07-19：静态领域规则第一批迁移

- 迁移器现在直接在构建期读取归档的 `canonAdditions.ts`、`martialSoulCategories.ts` 与 `martialSoulTiers.ts`，并复现旧 `catalog.ts` 的“唐三6岁”权重修正、去重增补、兽/器武魂分类子池、枪械剧情、高龄导师剧情与八个势力剧情池。生产运行时只加载 `legacyContent.generated.json` 和 `legacyMartialSoulRules.ts`，不导入归档源码，也不执行中文正则。
- 原始 276 池 / 2,500 项完整保留且可按原 pool/option ID 逐项寻址；经旧 catalog 规则扩展为 317 个迁移池 / 2,955 项，其中 63 个去重补充和 41 个虚拟池。
- 旧武魂分类与品阶在构建期固化为 27 个分类、292 项 `entityId -> tier` 元数据。`legacyMartialSoulRules.ts` 提供按 ID 查询品阶、最高品阶与分类，回归测试覆盖 T1/T6、双生最高阶和枪械分类。
- 枪械武魂剧情的武魂前置、导师年龄/40 级前置、势力剧情年龄/等级/性别前置均已生成结构化 predicate；静态称号、等级和导师身份均生成受限 effect。
- `tests/content/legacyMigration.test.ts` 从 3 增至 5 项，整个 Vitest 套件为 49 项。新增 `V03-MIGRATE-002`，并以归档 SHA、补充/虚拟池/分类/品阶/门槛及 backlog 为验收。
- 内容与 package 版本升至 `v0.3.2` / `0.3.2`。`content:validate` 现在将 280 条复杂原始约束和 40 条势力剧情阶段调度显示为 `pending: 320`、状态 `recovery-in-progress`；缺失文件、效果、引用等结构错误仍会直接失败。该状态不等同于发布认证通过。
- 未完成项：`engine.ts` 的完整战力/资格策略及 `machine.ts` 的势力阶段、特殊剧情和完整任务排程尚未接入；后续必须先消除这些 backlog，再运行新的 20 x 20 认证。

# 2026-07-19：engine 资格规则第二批迁移

- 迁移期将旧版武魂名称分类为 `type`、`attribute` 元数据；运行期新增 `policy.martial-soul-tier`、`policy.martial-soul-type`、`policy.martial-soul-attribute`、`policy.has-domain` 与 `policy.appearance-min-rank`，均只读取状态中的稳定实体 ID 与枚举参数。
- 迁移器把火/水/雷/冰/风/光/暗/生命/毁灭、剑/刀/枪/弓/龙族武魂门槛，以及极致/兽/器/本体/概念型武魂门槛编译为命名 policy；神考和领域门槛编译为现有 fact 或领域实体；原版容貌池的八个结果授予现有 appearance 实体，B/S 级门槛因此可由 policy 判断。
- 明确以阿拉伯数字写出的魂兽修为限制会编译为 `beast.cultivation` 比较，例如“要求30万年”变为 `gte 300000`；中文数词、路线称号、法则计数等无法无歧义转换的规则保持审计待办。
- 审计从首批静态迁移后的 280 条复杂条件 + 40 条阶段调度，下降为 230 条复杂条件 + 40 条阶段调度，即 `pending: 270`。内容仍为 `recovery-in-progress`，不得解释为认证通过。
- 新增候选分布回归，证明“火属性武魂限定”只在拥有编译后火属性实体时进入候选集；新增容貌与 30 万年门槛断言。全套 Vitest 现为 50 项。

# 2026-07-19：恢复长程验证标准重定

- 用户指出当前人类创建流程在选择“器武魂”后只显示“青钢剑武魂池”。根因不是原始器武魂数据缺失，而是 `CharacterSetupProcess` 仍指向 `setupContent.ts` 的一项占位池；迁移后的 `器武魂` 完整池有 89 项，却没有接入该流程。
- 旧长程验证只检查旅程能完成、终局和既有顺序审计，设置切片只使用正则匹配 `pool.setup.martial-soul.*`；它没有断言武魂类型到原始池的 ID 映射、候选数、选项集合或每个原版池的可达性，因此把占位路径误判为通过。
- 在继续修复前，新增 `docs/v0.3-long-run-recovery-standard.md` 并将其写入重构计划和 6 个 P0 验收用例。新标准要求 276 个池/2,500 个选项逐项保真、全部原始池的真实流程可达性、六类武魂入口矩阵，以及完整频率报告。频率报告必须包含全部原始池和零频项；所有零频池必须有真实受控路径，`blocked` 必须为 0。
- 本轮只定义验收契约，尚未把器武魂完整池切入流程，也未运行新的 100 次恢复样本；当前状态仍为 `recovery-in-progress`。

## 验收标准补充

- 长程恢复样本调整为两组固定 cohort：100 human 与 100 beast。这样人类成神率以完整 100 个固定 human seed 为分母，而不会把仅有 50 个样本的随机波动误判为系统比例。
- 每个 EventBatch 必须重放：抽中选项的结构化 effect 与实际 `stat.changed`、实体增删、时间、signal 和终局逐项相等，并在下一状态和传记投影中可见。原版文案中尚未结构化的副作用不得用 `effects: []` 混过审计。
- 等级审计将验证来源、边界、神考例外和回放一致性；战力审计要求恢复原版等级、魂环、武魂品阶、领域、魂骨、天赋/战斗特质组成的纯函数，禁止继续把仅 `level²/20` 当成合理战力。
- 100 个 human 样本的继承成神与自创成神合计比例必须为 45%-55%，报告所有 seed、终局和路径。每组至少抽检 20 份人物传记，覆盖所有已出现的 effect 类别、成神、死亡、化形及战力上下界样本。

# 2026-07-19：完整原始武魂设置池接入

- 修复 `CharacterSetupProcess`：兽、器、变异、概念、本体、极致六类武魂类型现在分别调度迁移的完整原始池，而非 `setupContent.ts` 中的 6 个单项示例池。器武魂入口从“青钢剑”一项恢复为 89 项迁移候选；其他入口分别为 136、9、13、13、33 项。
- 删除已经不再属于游戏流程的 6 个单项示例实体和池，防止内容浏览器或后续流程误用占位内容。原始池的所有 source option ID 继续可寻址，旧 catalog 扩展项仍保留为显式迁移记录。
- 迁移器为完整原始武魂池的每个选项追加 `signal.setup.martial-soul-selected`。因此授予武魂后，设置流程会稳定排程年龄设定，不会因接入完整池而中断。
- 新增六类类型到完整池的 Process 回归、候选总数、原始 option ID 保留和 signal 断言；随机的人类设置骨架同时断言抽取原始池、候选数大于 1、且随后进入年龄池。当前测试总数 52。
- 内容与 package 版本升级为 `v0.3.3` / `0.3.3`，内容审计为 344 pools / 3,050 options / 462 entities，迁移 backlog 仍为 270。已验证 `npm run content:migrate`、`npm run content:validate`、`npm test`（52/52）、`npm run build`、`npm run architecture:validate`、`npm run test:e2e`（7/7）；构建保留 1.30 MB raw / 193 KB gzip 的既有非阻断 bundle 警告。

# 2026-07-19：原版恢复长程执行记录（进行中）

## 魂兽成神规则澄清

- 用户明确约束：纯魂兽的唯一成神路径是百万年雷劫成功后自创兽神神位；不得进入人类继承神考、海神继承或人类自创神位。
- `GodTrialProcess` 与 `SeaGodProcess` 对 `route: beast` 直接拒绝；`reducer` 同时把该约束固化为事件不变量，拒绝纯魂兽的神考、非 `entity.godhood.beast` 神位授予、未完成百万年雷劫的兽神神位，以及任何非兽神的存活成神终局。
- `legacyProgressionProcesses` 新增绕行回归：神位感召、自创神考、海神九考、伪造神考事件、伪造人类神位和伪造成神终局均必须失败；仅“百万年雷劫已结算 -> 自创兽神神位 -> 兽神终局”可通过。
- 复核百万年神劫原始八个选项后，修复了“出现成功/神位字样即授予兽神”的迁移错误。现在只有文本明确为“自创兽神神位”的选项发出成神信号；“成功但未获神位”的两项走 `ending.beast-immortal`，失败项发出显式失败信号并按原始属性变化继续或死亡。
- 内容版本升至 `v0.3.6`。`npm run content:migrate` 保持 276 个原始池 / 2,500 个选项的来源基线，聚焦迁移与流程回归 37/37 通过，生产构建通过（主包 2.60 MB raw / 313.54 KB gzip 的既有非阻断警告仍在）。
- 新诊断 `simulation-archives/v03-recovery/diagnostic-v03-recovery-021/`：200/200 自动审计通过，human 成神 46/100，纯兽类无非 `entity.godhood.beast` 神位，覆盖为 209 observed / 23 controlled / 44 blocked。该代次只证明规则未破坏完整运行；因 `blocked > 0`，仍为失败诊断，不能认证。

## 证据与规则边界

- 恢复基线固定为 `1338d1b^:src/data/wheels.json`，SHA-256 `a15531384c18d428622596d1d96b645ded8a8d65e14e3d948c1c33ad1c024b1b`，静态规模为 276 个池、2,500 个选项、25 个标签。`npm run content:migrate` 保持逐项映射，迁移审计为 `unsupportedConstraints: 0`、`unresolvedStaticRules: 0`。
- 新增 `npm run simulate:recovery`：每代次固定运行 100 human + 100 beast，逐 EventBatch 重放结构化 effects，核对等级边界、战力快照、传记投影、全部 276 行频率表和受控证据。受控路径只使用 `run.start` 与 `turn.spin`，不会直接写 agenda、状态或调用 draw。
- 原始与扩展池严格分离。`legacyFlow.pools` 仅包含 276 个原始池；`legacyFlow.virtualPools` 承载武魂分类和其他扩展，不能替换原始盘面。

## 诊断与修复

- `diagnostic-v03-recovery-003`：自动审计 193/200，发现成长遭遇后预先排程下一等级池，遭遇或势力效果降低等级后会触发 `NoEligibleOptionError`。`humanProgressionProcess` 改为先完成遭遇及队列中的后续任务，再按最终等级续接。
- 结构化战力函数恢复等级、魂环、武魂品阶、领域、魂骨、天赋和战斗特质组成。T1 武魂在一级会因原公式四舍五入得到 0；为满足“已觉醒存活魂师战力必须为正”的恢复门槛，保留所有组成项并仅对已持有武魂的总值加显示下限 1。`tests/core/combatPower.test.ts` 覆盖该回归。
- `diagnostic-v03-recovery-005`：魂环新增侧链暴露“势力剧情完成时魂骨任务仍在队列中，最终队列清空后未继续成长”。流程现在以完成后 state 续接，不再假设任务排列顺序。
- 魂环链已真实接入：每个原始魂环池抽取后，按迁移期生成的环序号进入对应原始“魂兽类型”池；按年份进入十/百/千/万年魂骨概率池；肯定结果进入原始魂骨池；八十级前十万年魂环进入原版成长奖励池。运行期仅读取 `ringIndex`、`ringYears`、`soulBoneYears` 等生成元数据，不解析中文题名。
- 人类辅助链已接入每次时间跳跃的特殊成长概率/结果、16--22 岁杀戮之都和其肯定结果后的杀神领域。新增可重放的 `flow.stage-completed` 事件记录一次性流程节点，防止依赖已出队 task ID 去重。
- 受控扫描增加 `run.start({ route: 'random' })`，因此原始种族与时间线池也可由真实入口路径形成受控证据；它不参与 human/beast 频率或成神率的 100 人分母。
- 神考改为可审计的感召转盘和分段奖励推进：感召命中后仍进入原始 `99级神考触发`、神级/神位池与原始奖励池；奖励必须达到其生成的等级门槛后逐考完成，未使用测试后置成神或结束覆盖。

## 当前诊断结论

- 最新诊断：`simulation-archives/v03-recovery/diagnostic-v03-recovery-016/`。
- 自动副作用、等级、战力与事件重放：200/200 通过。
- 覆盖：176 observed、4 controlled、96 blocked。主要缺口为海神奖励 24、神考奖励 26、魂兽时间线遭遇 10、魂兽雷劫/进化 8，以及海神、献祭、高等级初始设定和专用剧情入口。
- 人类成神率：16/100（16%），低于 45--55% 门槛。当前感召及逐考链可运行，但仍需通过原版资格、死亡权重与流程节奏校准；不得通过筛选种子、强制终局或在样本后覆盖结果达标。
- 因 `blocked != 0` 且成神率未达标，R2/R3 未通过；该代次仅为诊断证据，不能用于发布认证或替代新的双 100 恢复代次。

# 2026-07-19：原版海神岛支线恢复

## 迁移契约

- 在 `scripts/migrate-v03-legacy-content.ts` 的构建期文本解析中新增海神岛专用语义：剧情 16 的前往选择、黄/紫/黑/顶级/海神九考等级、考核总数、五年计划的完成数/失败状态、23 个分级奖励的考次，以及“是否在神战前完成你还剩下的考核？”补考门。生产运行时不读取任何中文标题。
- `legacyFlow.generated.json` 新增 `progression.seaGod`：海神等级池、五年计划池、补考门、海神九考继承的原版一级神位实体，以及 23 个 `{ grade, exam, poolId }` 奖励映射。分级奖励与补考门合计覆盖原版 24 个海神奖励标签池。
- `GameState.progression` 新增结构化 `seaGodTrial`，并新增 `sea-god.started`、`sea-god.plan-selected`、`sea-god.plan-extended`、`sea-god.reward-progressed` 事件。Reducer 对考数范围、计划扩展和逐考单调性做回放校验。

## 实际流程与回归

- 新增 `SeaGodProcess` 并接入 `GameService`：剧情 16 选“前往”后排程原版海神考核；普通海神考核依次进入五年计划、原版奖励池和未完成时的原版补考门。计划选择会真实推进人物年龄和唐三时间各 5 年；补考选择“是”后继续剩余的原版奖励。
- 海神九考不使用占位奖励：按原题面“按照一级神考奖励完成神考”直接以迁移期标定的海神实体启动原版一级神考，并交给既有等级门槛奖励流程。
- 修复剧情 16 与普通成长并发排程：人类成长 Process 收到海神入口 signal 时等待 SeaGodProcess 调度，避免同一回合同时进入成长池和海神池。
- `tests/core/legacyProgressionProcesses.test.ts` 从 5 项扩展为 7 项，覆盖真实入口、黑级五年计划、年龄/时间副作用、逐考奖励、补考门延长计划及海神九考转一级神考。`tests/content/legacyMigration.test.ts` 锁定海神元数据、23+1 原版池映射和继承实体。执行 `npx vitest run tests/core/legacyProgressionProcesses.test.ts tests/content/legacyMigration.test.ts`：16/16 通过；`npm run build` 通过。

## 诊断代次 017

- 生成 `simulation-archives/v03-recovery/diagnostic-v03-recovery-017/`，全程由 100 human + 100 beast 的 `run.start -> turn.spin` 执行；自动副作用、重放、等级和战力审计为 200/200。
- 覆盖由代次 016 的 `176 observed / 4 controlled / 96 blocked` 提升到 `189 observed / 31 controlled / 56 blocked`。海神 24 池中 23 个已 observed 或 controlled；黑级第六考尚未命中，保留为 `blocked`，不能以单测或空效果替代。
- 人类成神率为 17/100，仍低于 45--55% 门槛。该代次状态 `failed`，只作为诊断证据；下一切片需恢复原版高阶神考入口与试炼节奏，并继续清理魂兽时间线、献祭、领域和专用剧情分支。

# 2026-07-19：海神五年成长与九考训练恢复

## 根因与约束

- 复查 `diagnostic-v03-recovery-022` 的 33 个 blocked 池后，定位到海神五年成长池与海神岛修行池虽然保留在内容中，却未由完整流程抽取：五年计划直接手写 `age + 5`、`tang-age + 5` 后进入奖励，海神九考则回退到扩展的通用训练池。
- 运行时不得依据中文标题判定路径。本轮仅在 `scripts/migrate-v03-legacy-content.ts` 构建期以原始题名解析，并生成 `legacyFlow.progression.seaGod.growthPoolId` 与 `trainingPoolId`；生产流程只消费稳定 pool ID。

## 实施

- `SeaGodProcess` 现在把“五年计划”结算为 `sea-god.plan-selected` 加原版五年成长任务。年龄、唐三时间和等级只由该原始池的结构化 option effects 落地；该池发出 `signal.human.growth-completed` 后，流程才调度第一份原版海神奖励。失败计划同样先经过五年池，再以无奖励完成海神阶段。
- `HumanProgressionProcess` 在活跃海神计划的计划、五年完成、奖励和补考信号上不再并发排程普通成长；海神阶段完成后才恢复普通成长。
- 海神九考在原版一级神考奖励尚未达到等级门槛时调度原版“海神岛修行”池。该池在迁移期显式发出训练完成 signal；一旦达到当前一级神考奖励门槛，奖励任务优先于训练任务，杜绝无限修行循环。
- 新增定向回归：五年计划不再手写时间、原始五年池后才进入奖励、活跃海神阶段不并发普通成长、九考使用原版修行池、以及奖励门槛达到后优先进入原版奖励池。迁移测试锁定两个生成 ID 和训练完成 signal。

## 验证与证据

- `v0.3.7`：`npm run content:migrate`、`npx vitest run tests/content/legacyMigration.test.ts tests/core/legacyProgressionProcesses.test.ts tests/core/gameService.test.ts`（37/37）、`npm run content:validate`、`npm run build` 通过。`diagnostic-v03-recovery-023` 覆盖提升到 210 observed / 39 controlled / 27 blocked，但 human-005 在一级神考奖励门槛后反复进入海神训练，自动审计 199/200，代次作废。
- 修复任务优先级并按恢复标准升版至 `v0.3.8`。同一聚焦测试 37/37、内容审计和生产构建再次通过；固定 seed `v03-recovery-human-005` 于第 71 回合以 `ending.god-ascension` 正常结束。
- `simulation-archives/v03-recovery/diagnostic-v03-recovery-024/`：200/200 自动审计通过，human 成神 46/100（46.0%），纯兽类规则未出现违规问题；覆盖为 210 observed / 39 controlled / 27 blocked。因 blocked 非零，状态仍为 failed，仅作为下一切片的诊断证据。
- 剩余 P0 集群：二级神考与 8 个奖励池、神战 1/2 与战后选择、三舞献祭、百万年兽神后的选择与海神时代事件 4、第二/三武魂魂环、完整领域、特殊成长、19--20 岁一年成长、以及自创神位剧情。每项仍需由 `run.start -> turn.spin` 的真实流程或可审计固定 seed 达到，不能通过 agenda 注入声明为 controlled。
