# 遗留问题

## v0.3.13 原版转盘恢复

更新时间：2026-07-20。

`v0.3.13` 已修正低等级继承神考排程、传记战力公式缺失和势力剧情标头被授予为特质的问题。新归档 `simulation-archives/v03-recovery/v0.3.13-god-gate-biography-composed-20260720/` 的自动审计为 200/200，覆盖为 217 observed、46 controlled、2 composed、11 blocked；但 human 成神率只有 14/100（14.0%），低于恢复标准的 45--55%，因此不是发布认证。

冻结前已通过 `npm run test:core`（83/83）、`npm run content:validate`、`npm run architecture:validate`、`npm run build` 和 `npm run test:e2e`（7/7）。恢复脚本现在会拒绝任何含已跟踪或未跟踪改动的工作树，确保归档记录的 commit 与实际运行代码一致。提交 `v0.3.13` 候选后，必须在干净工作树上重新生成恢复代次；旧归档不能作为候选认证证据。

静态迁移已通过：276/276 原始池、2,500/2,500 原始选项、25/25 标签均有稳定 ID 映射，未发现选项遗漏、未解析约束或未解析静态规则。

`simulation-archives/v03-recovery/v0.3.12-c6904b98e1ed-2026-07-19T18-10-00-816Z/` 的双 100 运行中，200/200 自动副作用、重放、等级及战力审计通过，但真实流程覆盖仍为 208 observed、31 controlled、37 blocked，因此该代次不是发布认证。

其中 `兽武魂`、`器武魂`两个原始父池不应直接抽取：它们已按旧版协议由“分类转盘 -> 分类子转盘”完整复用，子池选项和基础权重并集均经回归验证。恢复标准和新归档均将这两项定义为 `composed`，不是内容或选项缺失。

当前仍有 11 项缺真实流程或可复现受控路径，完整 pool ID、题名和频率记录见新归档的 `pool-frequency.json`。此外，继承神考门槛恢复为 70/80/99 后，需在不重新开放 20--60 级感召的前提下校准后续成长与神考节奏，使固定 human 样本回到 45--55% 成神率。当前按流程簇归类如下：

- 第二、三武魂九十级魂环；特殊成长；19--20 岁一年成长；自创神位；`未完待续`纯叙事终局池。
- 三舞献祭事件及武魂殿、史莱克双方结果。
- 紫级第五考奖励。
- 百万年兽神后的选择和海神时期限定事件 4。

处理要求：每一项只能通过从 `run.start` 到 `turn.spin` 的真实链路修复或建立固定 seed 证据；不得直接写入 agenda、编辑 state 或调用 draw 伪造 controlled。修复任一项后必须生成新的恢复代次，并更新本文件与 `docs/v0.3-implementation-log.md`。
